name: OIDC Continuous Deployment

# This workflow is triggered whenever commits are pushed to the main branch
on:
  push:
    branches:
      - 'patch-1'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

defaults:
  run:
    shell: bash
    # Define the working directory for all run steps in the workflow
    working-directory: ./web

jobs:

  deploy_production:
    environment: production
    name: 'Deploy to production'
    runs-on: ubuntu-latest
    
    steps:
    
    - uses: actions/checkout@v2
      name: 'Checkout repository'

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@05b148adc31e091bafbaf404f745055d4d3bc9d2
      with:
        ##### CHANGE ROLE-TO-ASSUME!
        role-to-assume: arn:aws:iam::265499128033:role/GitHubOidc
        role-session-name: ElasticBeanstalkDeployment
        aws-region: us-east-1

    - name: Deploy changes
      run: |
        brew install awsebcli
        eb deploy
